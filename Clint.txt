git rm <filename.format> (Removing file)
git add -u
touch <filename.format> (Create a file)
cat <filename> (Read the content of your filename)
git restore --staged <filename.format>
git restore <filename.format>
echo "Your file content" > <filename.format>
rm -rf .git (remove .git using bash)